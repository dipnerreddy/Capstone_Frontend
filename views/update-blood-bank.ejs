<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Blood Bank</title>
    <style>
        /* Reset basic styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #e0f7fa; /* Sky blue background */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 25px 0; /* Adding padding to body for top and bottom space */
        }

        /* Center the form inside a container */
        .form-container {
            background-color: #fff;
            padding: 2rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            width: 100%;
            max-width: 600px;
            text-align: center;
            margin-top: 25px; /* Top margin */
            margin-bottom: 35px; /* Bottom margin */
            position: relative; /* Position relative for the absolute positioning of the button */
        }

        .go-back-button {
            position: absolute;
            top: 15px;
            left: 15px;
            width: 40px;  /* Square shape */
            height: 40px; /* Square shape */
            background-color: #007bb5;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 0;
            text-decoration: none; /* Remove underline */
        }

        .go-back-button:hover {
            background-color: #005f8c;
        }


        h1 {
            margin-bottom: 1.5rem;
            font-size: 28px;
            color: #007bb5; /* Darker sky blue */
        }

        /* Style the form elements */
        label {
            display: block;
            text-align: left;
            margin-bottom: 0.5rem;
            color: #007bb5;
            font-weight: bold;
        }

        input[type="text"], input[type="number"] {
            width: 100%;
            padding: 0.75rem;
            margin-bottom: 1rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 16px;
        }

        button {
            padding: 0.75rem 1.5rem;
            background-color: #007bb5;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            width: 100%;
        }

        button:hover {
            background-color: #005f8c;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <a href="/dashboard" class="go-back-button">⬅</a>
        <h1>Update Blood Bank Info</h1>
        <p>Logged in as: <strong><%= email %></strong></p>
        <form action="/update-blood-bank" method="POST">
            <!-- <label for="email">Email:</label>
            <input type="text" id="email" name="email" value="<%= email %>" readonly> -->

            <label for="oPositive">O Positive:</label>
            <input type="number" id="oPositive" name="oPositive" required>

            <label for="oNegative">O Negative:</label>
            <input type="number" id="oNegative" name="oNegative" required>

            <label for="aPositive">A Positive:</label>
            <input type="number" id="aPositive" name="aPositive" required>

            <label for="aNegative">A Negative:</label>
            <input type="number" id="aNegative" name="aNegative" required>

            <label for="bPositive">B Positive:</label>
            <input type="number" id="bPositive" name="bPositive" required>

            <label for="bNegative">B Negative:</label>
            <input type="number" id="bNegative" name="bNegative" required>

            <!-- Add fields for other blood groups if necessary -->
            <button type="submit">Update Blood Bank Info</button>
        </form>
    </div>
</body>
</html>
